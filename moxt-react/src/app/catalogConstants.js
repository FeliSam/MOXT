/** Max rows pulled for the public marketplace listings catalog. */
export const LISTINGS_PUBLIC_LIMIT = 500

/** Max rows for other public catalogs (videos, parcels, jobs, …) in global pulls. */
export const PUBLIC_CATALOG_LIMIT = 50
